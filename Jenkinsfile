pipeline {
    agent any
    stages {
        stage('Check Version') {
            steps {
                bat "node --version"   
                bat "npm --version"    
            }
        }
    }
}
